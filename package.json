{
  "name": "financeapp",
  "version": "1.0.0",
  "private": true,
  "description": "FinanceApp - monorepo (backend NestJS + frontend React servidos por um unico processo)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "postinstall": "npm --prefix backend ci --no-audit --no-fund",
    "build": "npm --prefix backend run build",
    "start": "node backend/dist/main.js",
    "dev": "npm --prefix backend run start:dev & npm --prefix frontend run dev"
  }
}
